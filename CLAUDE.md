# CLAUDE.md

## 一、项目简介（Project Overview）
- 项目名称：openclaw源码拆解学习
- 一句话说明：项目名称：openclaw源码拆解学习，手写每个模块的逻辑实现openclaw的功能。
- 项目目标：
  - 目标 1：深度理解openclaw设计理念
  - 目标 2：学习设计openclaw思想
- 当前阶段：
  - [x] 原型 / MVP
  - [ ] 功能迭代
  - [ ] 性能优化
  - [ ] 上线运营

## 二、协作原则（How You Should Work）
- 回答语言：优先使用简体中文，技术名词可保留英文。
- 风格要求：解释要清晰、避免废话，必要时给简短示例。
- 不要擅自编造：遇到不确定的信息，请先向我提问澄清，而不是自己假设。
- 优先保证：正确性 > 可读性 > 优雅的实现 > 性能（除非特别说明）。

## 三、技术栈与运行方式（Tech Stack & Runbook）
### 前端
- 框架：React + TypeScript
- 构建工具：Vite
- UI：Tailwind CSS
- 目录约定（示例，可按项目修改）：
  - `src/components`：通用组件
  - `src/pages`：页面级组件
  - `src/hooks`：自定义 Hooks
  - `src/lib`：工具方法

### 后端
- 语言 / 运行时：Node.js 20
- 框架：Express / FastAPI / Spring Boot 等（按实际填写）
- 数据库：PostgreSQL / MySQL / MongoDB（按实际填写）
- ORM / 数据访问：Prisma / TypeORM / 自定义 DAO（按实际填写）

### 本地开发
- 安装依赖：`npm install`（或 npm / yarn）
- 启动开发环境：`npm dev`
- 运行测试：`npm test`



## 四、编码约定（Coding Conventions）
### 通用
- 命名风格：变量与函数使用小写驼峰 `camelCase`，类名使用大写驼峰 `PascalCase`。
- 每个文件只负责单一清晰的职责，超过 300 行优先拆分。
- 必要时添加简短注释，解释「为什么这样做」，而不是「这段代码做了什么」。



## 六、Git 与提交规范（Git Workflow）
- 分支命名：
  - 功能：`feature/简要描述`
  - 修复：`fix/简要描述`
  - 试验：`experiment/简要描述`
- 提交信息格式（参考）：
  - `feat: 新增用户登录功能`
  - `fix: 修复分页参数错误`
  - `chore: 更新依赖版本`
- 小步提交，保持每次提交可描述为一件清晰的事情。

## 七、AI 协作规则（How You Should Use Claude）
### 可以直接执行的任务（无需额外确认）
- ✅ 根据现有风格补充类型、注释和文档。
- ✅ 在不改变对外行为的前提下，重构代码结构。
- ✅ 为已有功能补充单元测试或简单集成测试。
- ✅ 修复明显的拼写错误、格式问题、简单逻辑错误。

### 需要先询问再执行的任务
- ⚠️ 修改业务逻辑或接口协议（请求 / 响应字段）。
- ⚠️ 引入新依赖库或删除现有依赖。
- ⚠️ 调整数据库表结构、索引或迁移脚本。
- ⚠️ 大规模重构目录结构或文件命名。

### 禁止的操作
- ❌ 编造不存在的接口、配置或环境变量名称。
- ❌ 提议在代码中硬编码密钥、密码、token 等敏感信息。
- ❌ 删除文件或大段代码而不说明原因（如需删除，请先标记为 deprecated 并解释）。

## 八、项目特定约束（Project-Specific Rules）
> 如果你的项目有特别重要的约束（例如：必须兼容某旧版本浏览器、必须满足某合规要求、对性能有特殊要求等），请在这里列出。

- 例：
  - 必须兼容：移动端主流浏览器最近两个大版本。
  - 接口响应时间：核心接口在 200ms 以内（正常负载）。
  - 日志中不得出现用户的个人敏感信息（手机号、邮箱明文等）。

> 每次修改后自己先总结，然后生成变更日志，并提交自动 git。